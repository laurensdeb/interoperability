{
  "name": "@laurensdeb/authorization-agent",
  "private": true,
  "scripts": {
    "commit": "commit"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^16.1.0",
    "@commitlint/config-conventional": "^16.0.0",
    "@commitlint/prompt-cli": "^16.1.0",
    "@commitlint/config-lerna-scopes": "^16.0.0",
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  }
}
